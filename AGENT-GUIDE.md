# 大字报｜Agent 使用指南

这份指南给能读取 Skill、处理文本并生成 SVG 的 Agent 使用。它把“大字报”从一个视觉风格，落实成可执行的输入、输出、飞书写入和验证契约。

## 1. 何时触发

用户提出以下任务时使用本 Skill：

- 要做“大字报”、结论海报、诊断总览、状态卡或行动摘要。
- 要把一段报告、选中文本或已有判断做成蓝米双色高对比画板。
- 要生成带画板的飞书云文档，尤其强调分享链接打开后的首屏主旨。
- 要重绘已有大字报画板，或修复文字贴边、层级混乱和正文重复。

流程图、因果图、架构图、网络图、时间线和二维矩阵交给 `geometry-board`。

## 2. 输入契约

Agent 至少需要拿到以下一种输入：

- 用户提供的结论、报告、选中文本或结构化材料。
- 已有飞书文档的标题、正文段落和画板位置。
- 已有画板的可读预览或 SVG，以及用户明确的修改要求。

开始前先写一句内部摘要：`读者看完要记住什么？`。如果材料无法形成一个可复述判断，先缩小范围或请求补充，不用装饰填空。

## 3. 输出契约

### 单张画板

默认交付：自包含 SVG、可选 PNG 预览和一句构图说明。画板只保留一个核心判断；完整证据、来源、置信度、替代解释和边界回到正文。

### 飞书云文档

用户要求生成云文档时，默认创建 4 张画板；只有 3 个独立阅读任务时创建 3 张：

1. `01 总览锚点`：紧跟标题，作为标题后的第一个正文内容块。独立讲清对象、核心判断、证据线索和下一步。
2. `02 证据地图`：紧跟证据或材料部分，说明证据来源、行为信号和指向。
3. `03 状态判断`：紧跟分析或状态部分，说明当前强弱、关键转折和待确认边界。
4. `04 行动落点`：紧跟行动部分，说明真实场景、具体动作、验收和触发条件。

第一张画板要能脱离“见下文”复述整篇文档主旨。其余画板按正文主题分散插入，不能统一追加到文档末尾。

默认四板描述文档的阅读任务；单张画板中的同级状态、动机或行动元素可按材料使用 3–5 项自适应网格，两套数量规则彼此独立。

## 4. 执行顺序

1. 读取本 Skill；生成云文档时继续读取 [references/visual-style.md](references/visual-style.md)。
2. 提炼一个核心判断，确认受众是本人、管理者、团队还是公开读者。
3. 选择构图并压缩文案；标题写判断，块内写关键词和短证据。
4. 生成自包含 SVG。先判同级并列、主从层级或步骤顺序；对 3–5 项同级元素按数量和内容密度回排：3 项三列或纵向堆叠，4 项四列或 2×2，5 项五列或居中的 3+2。主从关系才可异形。遵守对齐、对比、排列、亲密，以及颜色、字号、边框和安全留白规则。
5. 运行 `whiteboard-cli --check`，处理溢出、重叠、遮挡、文字贴边和缩小后不可读问题。
6. 用户确认后再写入飞书；按正文标题和段落建立“画板主题 → 插入锚点”映射。
7. 写入后回读文档块树、每张画板的原始节点和预览，确认数量、顺序、位置和链接。
8. 分开报告“已生成”“已插入”“已回读验证”；任何一项缺失都保留准确状态。

## 5. 飞书写入边界

- 使用当前用户明确授权的飞书账号和租户；命令遵守对应 `lark-doc` / `lark-whiteboard` Skill 的身份要求和 `--as user` 约定。
- 先读取文档标题层级、段落顺序和已有画板，再决定插入点；不能凭标题猜位置。
- 画板插入能力不可用时，交付 SVG/PNG 源文件并明确“已生成、未插入云文档”；不要用静态图片链接冒充原生画板。
- 覆盖已有非空画板前说明视觉重绘会损失原有语义层级、节点类型和连线绑定，并取得确认。

## 6. 验证门槛

- SVG 可解析，且本地渲染成功。
- `whiteboard-cli --check` 的 `textOverflow`、`nodeOverlap` 和 `textOcclusion` 均为 0。
- 普通卡片文字到边框内侧至少 40px；标题、编号和核心判断至少 48px；边界说明至少 24px。
- 3–5 项同级元素的宽高差不超过 2px，且内边距、编号/标题锚点、边框、基线和间距一致；最后一项不能因空间不足被压窄、缩字或删减层级。空间不足时按数量回排：3 项纵向堆叠、4 项 2×2、5 项居中 3+2。
- 云文档回读顺序为：`标题 → 01 总览锚点 → 正文第一部分`。
- 3 张或 4 张画板的数量与独立阅读任务一致；每张画板都对应唯一正文主题。

## 7. 停止条件

遇到以下情况时停在可验证状态，并报告缺口：

- 材料没有明确判断，无法压缩成一张自洽总览。
- 证据不足以支持某个结论；把它标为待确认，不替用户补事实。
- 飞书目标文档、账号、租户或写入权限无法确认。
- 画板文本无法在安全区内排下；增高、换行或删减内容后仍不成立时，回到正文。

## 8. 调用示例

```text
Use $dazibao to turn this conclusion into a high-contrast Feishu whiteboard poster.
Use $dazibao to turn this report into a Feishu document with four boards, with the first board immediately after the title.
Use $dazibao to repair the card padding and re-check the rendered text bounds.
```
