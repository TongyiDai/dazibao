---
name: dazibao
description: 将结论、证据、判断和下一步动作制作成蓝米双色、超大字号、深蓝边框的飞书大字报画板或自包含 SVG；生成飞书云文档时，按标题后首张总览画板、后续证据/判断/行动画板的顺序创建默认 4 张画板。用户要求“大字报”、参考本套蓝米版式、强调结论、制作证据分栏、状态三卡片、行动三卡片、生成带首屏总结画板的飞书云文档、重绘现有画板或把文字内容放进高对比海报时使用；关系密集的流程图、架构图、网络图和一般几何图示使用 geometry-board。
---

# 大字报

## Overview

把一张图做成一眼能读懂的结论海报：先用超大标题给判断，再用蓝色重点块、白色证据块和少量结构化卡片承载证据、状态或动作。默认输出自包含 SVG，按需导出 PNG 并写入飞书画板；正文继续承载完整背景、引用和细节。

跨 Agent 的触发、输入输出、四板文档契约、验证顺序和停止条件见 [AGENT-GUIDE.md](AGENT-GUIDE.md)。执行任务时先遵守本文件的视觉与写入规则，再按需读取视觉参考。

## 适用边界

- 适用：结论海报、员工/客户诊断总览、能力判断、状态盘点、行动建议、复盘摘要、对比结论、需要在群聊或文档中快速阅读的强观点图示。
- 交给 `geometry-board`：流程、因果、依赖、系统架构、网络关系、时间线、二维矩阵或需要点线表达的复杂关系。
- 交给 `lark-doc` / `lark-whiteboard`：飞书文档读取、文档锚点定位、已有画板查询、写回和回读；本 Skill 负责版式与画面内容。
- 画板只保留一个核心判断。证据、完整解释、来源和不确定性放在正文或详情区。

## 工作流程

1. 读取用户文字、选中文本或已有画板；先写出一句内部摘要：`读者看完要记住什么？`。
2. 先判信息关系：同级并列、主从层级或步骤顺序。四个同级元素用等规格四列或 2×2；主从关系才允许异形。再选主构图：总览用“左侧主题 + 右侧问题/结论”；证据用“主题—证据—判断”分栏；状态和行动用“三张编号卡片”；对比用“蓝色重点面 + 白色基准面”。详细 token 和尺寸见 [references/visual-style.md](references/visual-style.md)。
3. 压缩文案：标题写判断，副标题写适用范围，块内写关键词和一个短说明；优先使用动词和真实场景；删除与正文重复的导语。
4. 生成自包含 SVG。使用 `<text>`、`<rect>`、`<line>`、`<polyline>` 等可解析元素；禁止外部图片、脚本、复杂滤镜、渐变和圆角卡片墙。若同时使用 geometry-board 的 Scene JSON，先运行其 `scripts/validate_scene.py`。
5. 本地渲染并检查：
   ```bash
   npx -y @larksuite/whiteboard-cli@^0.2.13 -i ./dazibao.svg -o ./dazibao.png -f svg
   npx -y @larksuite/whiteboard-cli@^0.2.13 -i ./dazibao.svg -f svg --check
   ```
   处理所有文字溢出、节点重叠、遮挡、边界贴合和缩小后不可读的问题，再交付预览。
6. 用户确认后再写入飞书。新画板按 `lark-doc` / `lark-whiteboard` 流程插入；已有非空画板使用 SVG 覆盖前，明确说明视觉重绘会丢失原有层级、节点类型、连线绑定等语义，并取得用户确认。所有飞书命令使用正确的用户身份和 `--as user`，写入后导出预览、读取原始节点并回读文档位置。

## 飞书云文档编排规则

- 当输出目标是飞书云文档时，默认创建 4 张画板；材料只支撑 3 个独立阅读任务时创建 3 张。不能为了凑数拆分重复信息。
- 第 1 张画板固定为“总览锚点”，紧跟文档标题，作为标题后的第一个正文内容块；画板前不放摘要段、导语或空白占位。它要独立讲清对象、核心判断、关键证据线索和下一步落点，用户只打开分享链接即可获得全文主旨。
- 第 2 张画板放在证据或材料部分之后，表达“证据从哪里来、指向什么”；第 3 张放在分析或状态部分之后，表达“当前判断、强弱和待确认边界”；第 4 张放在行动部分之后，表达“真实场景、具体动作、验收触发”。每张画板只承担一个阅读任务。
- 三张画板时采用“总览锚点 → 证据与判断 → 行动落点”；四张画板时采用“总览锚点 → 证据地图 → 状态判断 → 行动落点”。画板必须紧跟所解释的正文，不能全部堆到文档末尾。
- 第 1 张画板不得依赖“见下文”、长链接或只有正文才有的上下文；来源、完整证据、置信度和限制继续写在正文，画板只保留可独立复述的精要结论。
- 写入后必须回读文档块树和每张画板预览，确认顺序为“标题 → 第 1 张画板 → 正文第一部分”，且画板数量、位置、链接和视觉检查结果一致；任一项缺失都不能报告为已完成。

## 内容与版式规则

- 使用蓝色 `#375dfe`、米色 `#fdf0e0`、深蓝 `#1a2240`、白色 `#ffffff`；重点边框可用 `#2741c0`。
- 默认画布使用 1680px 内容宽度，16:9 附近比例；大标题 56–120px，正文 24–34px，底部边界说明不超过 24px。
- 使用 Noto Sans SC、苹方、思源黑体或系统无衬线；标题和核心判断加粗，文字层级控制在三层以内。
- 外边距约 70px；边框 3–4px；不使用渐变、阴影、纹理、圆角、彩色胶囊或装饰图标。
- 卡片内边距是硬约束：普通卡片四边至少 40px；大标题、编号和核心判断到边框内侧至少 48px；底部边界说明到边框内侧至少 24px。空间不足时增高卡片、换行或缩小内容，不得压缩安全距离。
- 留白按文字实际可见边界测量，不能只看 `<text>` 的起点、锚点或基线；边框厚度、字体字面外扩和变换后的实际边界都要计入检查。
- 每张卡片先建立内缩安全区，再放置文字；4px 边框下，文字布局坐标至少距卡片几何边缘 42px，标题和编号至少 50px，确保渲染后仍达到上述内侧留白。
- 排版同时遵守四项设计原则：对齐、对比、排列、亲密。
  - 对齐：同一组元素共用外缘、文字起点、标题/编号基线和箭头中心线。
  - 对比：用蓝白反差、字重或线条表达重点；同级元素不得靠缩小其中一项制造重点。
  - 排列：同级元素重复使用同样的宽高、内边距、边框、字号层级和间距。
  - 亲密：有直接关系的信息放进同一安全区；无直接关系的信息用卡片边框、留白或分区明确分开。
- 四个同级元素必须等宽等高，内边距、标题/编号锚点和卡片间距一致；外框宽高差不超过 2px。空间不足时改为 2×2，不得压缩第四项、降低其字号或让它失去完整内容。
- 仅当内容存在明确主从层级时允许卡片异形；此时必须在标题、分区或连线中让读者一眼看出层级，不能把并列内容伪装成主从。
- 蓝色承载核心判断、重点证据或当前行动；米色承载画布和普通证据；白色承载基准、解释或非重点项。
- 文字优先左对齐；编号卡片使用 `01 / 02 / 03`；同一组卡片保持相同内边距、边框和基线。
- 一张画板最多保留 1 个核心判断、3 个主要分组、6–8 个短文本块；长解释放入正文。
- 先过“3 秒复述测试”：只读标题和蓝色重点块，读者应能说出结论、对象和下一步。

## 系列画板

- 多张画板保持同一颜色、字体、边框和留白系统，改变内容构图承担不同阅读任务。
- 开头优先使用分栏总览作为锚点；中段使用证据表或状态卡；结尾使用行动卡。
- 连续两张画板不要使用完全相同的阅读方向和卡片骨架；信息关系变化时再变化构图。
- 画板与正文相邻时，正文保留证据、链接、置信度和边界说明，删除重复画板标题的句子。

## Gotchas

- `#375dfe` 是本 Skill 的重点蓝，不能自动替换成 geometry-board 的 `#2F6BFF`。
- 大标题很容易超出飞书实际画布；必须同时跑 `whiteboard-cli --check` 和远端预览。
- 渲染后必须检查每个文字块与卡片边缘线的实际最小距离：普通文字 ≥ 40px，标题/编号/核心判断 ≥ 48px，边界说明 ≥ 24px；任一侧不足即视为失败，必须回退布局后重渲染。
- 看到四个并列元素时，先做等规格检查：宽高、内边距、标题/编号位置、卡片间距和底边必须一致。第四项变窄、变矮或字号更小，均视为版式失败；空间不足改为 2×2。
- SVG 导入会把语义结构简化为视觉节点；已有画板覆盖属于有损编辑，不能把“视觉一致”表述成“结构保留”。
- 画板中不要塞完整证据段落、会议原话或长行动清单；这些内容应回到文档正文。
- 若只有零散信息、没有一个可复述的判断，先缩小问题范围，不要用装饰填满画布。
- 云文档生成失败或画板插入能力不可用时，明确报告“已生成 SVG / 未插入云文档”，保留可交付源文件，不用静态图片链接冒充原生画板。

## 交付

- 默认交付 `SVG + PNG 预览 + 一句话构图说明`；用户要求写入时再交付飞书文档/画板链接。
- 结果中区分“已生成”“已插入”“已回读验证”；不要只凭 CLI 成功消息宣称完成。
- 需要复用或修改时，优先保留源 SVG；删除、覆盖或移动已有画板前先确认目标和权限。
